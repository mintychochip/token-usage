{
  "name": "token-usage",
  "description": "Report jcode token usage to the shared Rust store. jcode can also depend on token-usage-adapters directly.",
  "version": "0.1.0",
  "reporter": "token-usage-reporter"
}
